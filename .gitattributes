#
# .gitattributes
#

#
# Common settings that generally should always be used with your language specific settings
#
# Stolen from: https://github.com/gitattributes/gitattributes/blob/master/Common.gitattributes

# 1. CORE RULE: Auto-detection
# Text files are normalized to LF in the repo, but extracted
# using the OS native format (CRLF on Win, LF on Linux).
* text=auto

# 2. DOCUMENTS AND DATA (Let text=auto handle these)
*.md        text
*.txt       text
*.json      text
*.xml       text
*.yaml      text
*.yml       text
*.toml      text
*.csv       text

# 3. LINUX/UNIX SCRIPTS (Always LF, even on Windows)
# Prevents "command not found" errors in bash caused by invisible CR characters.
*.sh        text eol=lf
*.bash      text eol=lf
*.zsh       text eol=lf
*.py        text eol=lf
*.vim       text eol=lf
.vimrc      text eol=lf
*.ipynb     text eol=lf

# 4. WINDOWS SCRIPTS (Always CRLF)
# Essential for files that Windows interprets strictly.
*.bat       text eol=crlf
*.cmd       text eol=crlf
*.ps1       text eol=crlf
*.psm1      text eol=crlf
*.psd1      text eol=crlf

# 5. BINARIES (Never modified)
*.png       binary
*.jpg       binary
*.jpeg      binary
*.gif       binary
*.ico       binary
*.pdf       binary
*.zip       binary
*.gz        binary
*.7z        binary
*.pyc       binary

# 6. SPECIFIC EXCEPTIONS
# Files where line endings are part of the data (e.g., patches)
*.patch     -text

# 7. EXCLUDE FROM EXPORT
.gitattributes  export-ignore
.gitignore      export-ignore
.gitkeep        export-ignore

